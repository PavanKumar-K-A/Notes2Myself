// Description: The 'return' statement in JavaScript Functions

// 1. The return statement can be used to cause the function to return early.
// 2. When return is executed, the function returns immediately without executing the remaining statements.
// 3. A function always returns a value. If the return value is not specified, then 'undefined' is returned.
// 4. If the function was invoked with the new prefix and the return value is not an object, then 'this' (the new
//    object) is returned instead.

function f() {
}

console.log("Return value:", f()); // undefined

console.log("TODO: Add a couple of more concrete examples"); // undefined
